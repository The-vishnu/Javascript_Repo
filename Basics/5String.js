const name = "Tony "
const repoCount = 50

console.log(`hello my name is ${name} and my repo count is ${repoCount}`)

const name2 = new String('iron man')

console.log(name2[0])
console.log(name2.length)
console.log(name2.toUpperCase())